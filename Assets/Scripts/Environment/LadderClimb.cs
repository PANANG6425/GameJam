using UnityEngine;

// [เลิกใช้แล้ว] ระบบปีนบันไดถูกย้ายไปรวมใน PlayerController.cs แล้ว
// ไฟล์นี้ไม่ทำอะไร (no-op) — ลบไฟล์นี้ทิ้งได้เลย และอย่าแนบ component นี้กับ Player
public class LadderClimb : MonoBehaviour { }
